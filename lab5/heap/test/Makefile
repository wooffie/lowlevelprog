
# Объектные файлы необходимые для сборки тестовой программы
# Тут нужен файл с методами библиотеки т.к. тестируются функции которые не входят в библиотеку и не должны быть доступны обычным пользователям (а массив можно трогать лол)
# Из-за этого же было решено отказаться от зависимости от файла библиотеки

OBJS= test.o  \
      CuTest.o \
	  ../src/heap.o

# Переопределяем имя выходного файла

NAME= test.exe

# Главная цель, для её выполнения надо собрать exe
# Выполняем файл с тестовой программой и выходим
all: $(NAME)
	$(NAME)

# Рецепт для сборки exe

$(NAME): $(OBJS)
	gcc $(CFLAGS) -o $(NAME) $(OBJS)

# Рецепт для компиляции объектников

%.o: %.c
	gcc $(CFLAGS) -c -o $@ $<

.PHONY: clean

# Цель очистки от файлов полученных в ходе работы компилятора

clean:
	$(RM) *.o
	$(RM) $(NAME)